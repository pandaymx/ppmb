---
title: Windows 基本操作
icon: ppmb icon-windows
order: 8
category:
  - 入门
tag:
  - 环境变量
  - 简单操作
isOriginal: true
---

## 创建文件和文件夹

打开资源管理器，右键单击空白区域，点击新建，创建需要的文件夹或文件即可。

## 文件扩展名

有时需要更改文件扩展名，但默认是不显示的。如果有其他的编辑器可以通过其他编辑器来另存为，也可以将文件扩展名不隐藏来查看。

选中 `···` &rarr; `选项` &rarr; `查看` &rarr; `隐藏已知文件类型的扩展名`。